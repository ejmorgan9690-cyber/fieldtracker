import { createClient } from '@supabase/supabase-js'; const s = createClient('https://owygcutjqfbjiihpevfr.supabase.co', 'sb_publishable_SRExcUhLmEReJ-W99l5Kjg_ug_cjCur'); async function test() { const { data, error } = await s.from('production_logs').select('*'); if (data) { const mapped = data.map(row => ({ id: row.id, inspector: row.inspector_name, date: row.service_date, taskType: row.task_type, rdtSection: row.section, route: row.route, location: row.location, footage: row.footage, boreNumber: row.task_type === 'Bore' ? row.spec_number : null })); console.log('MAPPED:', mapped); } } test();
